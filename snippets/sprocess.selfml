(name [synchronous process])

(trigger
    (tab spro))

(only-in source.vhdl)

(snippet [%{1="identifier"} : process( %{2="clock"} )
begin
	if( rising_edge(%{2="clock"}) ) then
		%{0}
	end if ;
end process ; -- %{1="identifier"}])